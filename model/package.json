{
  "name": "model",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {
    "nats": "^2.10.2",
    "cloudevents": "^6.0.3",
    "nats-jetstream-transport": "^0.3.3",
    "@nestjs/common": "^9.2.1"
  },
    "scripts": {
    "test": "exit 0",
    "build": "rimraf dist && tsc",
    "watch": "tsc -w",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.4"
  }
}
